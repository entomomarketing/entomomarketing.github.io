Original logo files as supplied.

Both are PNG rasters embedded inside an SVG wrapper, not true vector art —
which is why they are 478KB and 4.5MB. Nothing on the site links to them;
they are kept here only as the source of record.

The files the pages actually use are ../entomo-ai.{webp,png} and
../entomo-strata.{webp,png}, rendered from these at 1200px wide (19KB and
82KB). If true vector versions become available, replace these and re-export:
the hero renders the lockup at up to 540px, so 1200px covers 2x displays.

Note: the strata lockup's "entomo" wordmark is near-black (#101820) and
disappears on dark backgrounds. It is only placed on light ground. A light
variant would be needed before using it on a dark section.
